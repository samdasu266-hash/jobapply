# noindex 는 크롤러가 페이지를 읽을 수 있을 때만 듣는다. Disallow 로 막아
# 버리면 그 태그를 아예 못 읽어서, 어디선가 링크가 걸리면 제목·본문 없이
# 주소만 검색 결과에 남는다. 저장소가 공개라 그 링크는 이미 존재한다.
# 그래서 읽는 것은 허용하고, 색인은 index.html 의 noindex 로 막는다.
User-agent: *
Allow: /
